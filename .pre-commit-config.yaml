repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.0
    hooks:
      # markdown добавлен намеренно: ruff форматирует python-блоки внутри .md,
      # и CI (`ruff format --check src tests scripts`) это проверяет. Без него
      # хук молчал локально, а сборка падала на README со сниппетом.
      - id: ruff-format
        types_or: [python, pyi, jupyter, markdown]
      - id: ruff
        args: [--fix]

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-toml
      - id: check-merge-conflict
      - id: check-added-large-files
        # taxonomy.json ~64 КБ, обложки и СУБД в репозиторий не попадают
        args: [--maxkb=256]
      - id: detect-private-key
      - id: mixed-line-ending
        args: [--fix=lf]
